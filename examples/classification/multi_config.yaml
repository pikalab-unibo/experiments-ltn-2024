instance:
  - flower
  - species 

features:
  flower:
    - sepal_length
    - sepal_width
    - petal_length
    - petal_width
  species:
    - species

data:
  multi:
    instances: [flower]
    targets: [species]
    path: datasets/iris_training.csv
    batch_size: 32

constants:
  - setosa : 0
  - versicolor : 1
  - virginica : 2

predicates:
  Iris:
    args: [flower]
    structure:
      type: feed-forward
      layers:
        - in: [flower, 4 * flower]
          activation: elu
        - hidden: [4 * flower, 4 * flower]
          activation: elu
        - hidden: [4 * flower, 2 * flower]
          activation: elu
        - out: [ 2 * flower, 3]
          activation: softmax 

constraints: 
- all flower. ((species == setosa) -> Iris(flower, output) and output[0])
- all flower. ((species == versicolor) -> Iris(flower, output) and output[1])
- all flower. ((species == virginica) -> Iris(flower, output) and output[2])